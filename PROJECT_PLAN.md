# План разработки ThinkerBot

План составлен на основе истории коммитов проекта (6 коммитов, 2026-04-09 — 2026-04-10)

---

## Этап 1: Базовая реализация (Initial Commit)

**Коммит:** `6c0b8cb` — "Initial commit: ThinkerBot (Telegram bot with local LLM integration)"

### Задачи:
- ✅ Создать структуру проекта по SDD (Spec Driven Development)
- ✅ Реализовать архитектурные слои:
  - `app/handlers/` — обработка сообщений Telegram
  - `app/services/` — бизнес-логика (LLM service)
  - `app/clients/` — внешние API (Ollama client)
  - `app/core/` — конфигурация, ошибки, логирование
- ✅ Настроить асинхронную архитектуру (async/await)
- ✅ Реализовать обработку ошибок с fallback-ответами
- ✅ Настроить структурированное логирование
- ✅ Написать unit-тесты (16 тестов)

### Файлы:
```
app/
├── main.py              # точка входа, polling
├── core/
│   ├── config.py        # конфигурация из .env
│   ├── errors.py        # кастомные исключения
│   └── logger.py        # structured logging
├── handlers/
│   └── message_handler.py
├── services/
│   └── llm_service.py
├── clients/
│   └── ollama_client.py
tests/
├── test_bot.py
└── test_llm.py
```

---

## Этап 2: Исправление таймаутов (Commit #2)

**Коммит:** `7721f05` — "Update timeout configuration for slow LLM models"

### Проблема:
Модели `qwen3.5:4b` и крупнее не успевали генерировать ответ за 30 секунд

### Задачи:
- ✅ Увеличить read timeout до 120 секунд
- ✅ Использовать `httpx.Timeout` с раздельными настройками:
  - `connect=10.0` — подключение
  - `read=120.0` — чтение ответа (LLM генерация)
  - `write=30.0` — отправка запроса
  - `pool=10.0` — ожидание в пуле
- ✅ Добавить переменную окружения `TIMEOUT_READ`
- ✅ Исправить конструктор `OllamaClient` для использования `DEFAULT_TIMEOUT`

### Изменённые файлы:
- `app/clients/ollama_client.py`
- `app/core/config.py`
- `.env.example`

---

## Этап 3: Welcome-экран (Commit #3)

**Коммит:** `c6e1db5` — "Add welcome screen with wallpaper and model display names"

### Задачи:
- ✅ Добавить команду `/start` с отправкой картинки (`wallpaper.jpg`)
- ✅ Добавить команду `/help` с информацией о боте
- ✅ Реализовать команду `/model` с inline-клавиатурой
- ✅ Создать человеко-читаемые названия моделей:
  | Отображение | Модель |
  |-------------|--------|
  | турба | qwen2.5:3b |
  | турба версия 2 | qwen3.5:0.8b |
  | оптимальная | qwen3.5:2b |
  | точная | qwen3.5:4b |
  | умная | gpt-oss:20b |

### Новые файлы:
- `img/wallpaper.jpg` — приветственная картинка
- `img/icon.jpg` — иконка бота (установлена в Telegram)

### Изменённые файлы:
- `app/main.py` — handlers для `/start`, `/help`, `/model`

---

## Этап 4: Лимит длины сообщений (Commit #4)

**Коммит:** `71df3a7` — "Fix Telegram message length limit issue"

### Проблема:
Telegram ограничивает сообщения 4096 символами

### Задачи:
- ✅ Добавить `num_predict: 512` в payload Ollama (позже изменено)
- ✅ Реализовать утилиту `split_text()` для разбивки текста
- ✅ Отправлять ответы частями при превышении лимита

### Изменённые файлы:
- `app/clients/ollama_client.py`
- `app/handlers/message_handler.py`

---

## Этап 5: UX улучшение (Commit #5)

**Коммит:** `51d3965` — "Add UX improvement: show 'Thinking...' status"

### Задачи:
- ✅ Показывать "⏳ Thinking..." перед началом генерации
- ✅ Удалять сообщение "Thinking..." после получения ответа
- ✅ Улучшить пользовательский опыт (пользователь видит, что бот работает)

### Изменённые файлы:
- `app/handlers/message_handler.py`

---

## Этап 6: Исправление обрезания ответов (Commit #6)

**Коммит:** `71256fd` — "Fix truncated responses: remove num_predict limit"

### Проблема:
`num_predict: 512` обрезал ответы на середине

### Задачи:
- ✅ Установить `num_predict: -1` (без ограничений)
- ✅ Модель сама решает, когда закончить ответ
- ✅ `split_text()` продолжает защищать от лимита Telegram

### Изменённые файлы:
- `app/clients/ollama_client.py`

---

## Этап 7: Stateful история чата (реализовано после Commit #6)

**Описание:** Добавление управления контекстом и историей диалогов для stateful-взаимодействия

### Задачи:
- ✅ Создать модуль `app/history/history_manager.py` для per-user хранения истории
- ✅ Реализовать лимиты: `max_messages`, `max_tokens`, `summarize_trigger`
- ✅ Добавить автоматическую суммаризацию при превышении лимитов
- ✅ Создать `app/core/system_prompt.py` для инъекции системного промпта
- ✅ Добавить логирование полного контекста в `logs/context.log`
- ✅ Обновить `MessageHandler` для работы с историей
- ✅ Обновить `LLMService` для обработки контекста
- ✅ Добавить переменные окружения для настройки истории
- ✅ Реализовать команды `/sys` и `/sysreset` для управления системным промптом
- ✅ Добавить параметр `ENABLE_HISTORY` для включения/выключения истории

### Новые файлы:
- `app/history/history_manager.py` — управление историей чата
- `app/core/system_prompt.py` — провайдер системного промпта
- `logs/context.log` — лог контекстов

### Изменённые файлы:
- `app/handlers/message_handler.py` — интеграция с HistoryManager
- `app/services/llm_service.py` — обработка контекста и логирование
- `app/core/config.py` — добавлены настройки истории
- `app/main.py` — инициализация HistoryManager
- `.env.example` — добавлены переменные окружения

---

## Итоговая статистика

| Метрика | Значение |
|---------|----------|
| Всего коммитов | 6 |
| Файлов в проекте | 30+ |
| Строк кода (без тестов) | ~800 |
| Unit-тестов | 16 |
| Команд бота | 6 (`/start`, `/help`, `/model`, `/sys`, `/sysreset`, текстовые сообщения) |
| Моделей LLM | 5 |
| Поддержка истории | ✅ Stateful с суммаризацией |
| Логирование контекста | ✅ В logs/context.log |

---

## Архитектура (итог)

```
┌─────────────┐
│  Telegram   │
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│  Bot Handler    │  ← /start, /help, /model, /sys, /sysreset, text
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ HistoryManager  │  ← per-user history, summarization
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│  LLM Service    │  ← error handling, fallbacks, context logging
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Ollama Client   │  ← HTTP API, timeout, split_text
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│  Ollama Server  │  ← локальная LLM
└─────────────────┘
```

---

## Следующие шаги

### ❌ Не реализовано (из старого плана)
- ❌ Rate limiting для пользователей
- ❌ Очередь запросов (asyncio.Queue)
- ❌ Кэширование частых запросов
- ❌ Streaming ответов от LLM
- ❌ Админ-панель для статистики

### ✅ Реализовано (добавлено после старого плана)
- ✅ Поддержка контекста диалога (stateful история с суммаризацией)
- ✅ Логирование полного контекста в logs/context.log
- ✅ Управление системным промптом (/sys, /sysreset)
- ✅ Пер-user хранение истории в памяти
- ✅ Автоматическая суммаризация при превышении лимитов

### ❌ Не реализовано (на основе legacy-warning.md)

#### Приоритет 1 (высокий приоритет)
- ❌ Постоянное хранилище истории (Redis/SQLite вместо in-memory)
- ❌ Вращение логов (log rotation) для context.log
- ❌ Асинхронное логирование (aiofiles) для избежания блокировки event loop

#### Приоритет 2 (архитектура)
- ❌ Выделение ConversationService для оркестрации истории
- ❌ Интерфейс IContextManager для сменяемых бэкендов хранилища
- ❌ Интерфейс ILogger для декапсулирования логирования

#### Приоритет 3 (оптимизация)
- ❌ Точный подсчёт токенов (интеграция с tiktoken или Ollama tokenizer)
- ❌ Асинхронная суммаризация (в фоне)
- ❌ Мониторинг памяти и метрики
- ❌ TTL для неактивных пользователей (очистка истории после N часов)
- ❌ LRU eviction для наименее активных пользователей

#### Приоритет 4 (новые функции)
- ❌ Per-user системные промпты (настройка поведения для каждого пользователя)
- ❌ Поддержка thread-диалогов
- ❌ Экспорт/импорт истории диалогов
- ❌ Поиск по истории диалогов

---

*Документ создан: 2026-04-10*
*Обновлён: 2026-04-16 (добавлен Этап 7 и новые задачи)*
*На основе: git log --reverse, context-dump.md, legacy-warning.md*
